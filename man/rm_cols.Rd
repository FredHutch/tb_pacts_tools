% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_cleaning_functions.R
\name{rm_cols}
\alias{rm_cols}
\title{Remove Empty Columns from a Data Frame}
\usage{
rm_cols(my_data, tolower = TRUE, verbose = TRUE)
}
\arguments{
\item{my_data}{A data frame to clean}

\item{tolower}{Logical. If TRUE (default), convert all column names to lowercase}

\item{verbose}{Logical. If TRUE (default), print the number of columns dropped}
}
\value{
A data frame with empty columns removed and optionally lowercased column names
}
\description{
Removes columns that contain only NA values or only empty strings (for character columns).
Optionally converts all column names to lowercase.
}
\examples{
# Create example data with empty columns
df <- data.frame(
  Name = c("Alice", "Bob", "Charlie"),
  Age = c(25, 30, 35),
  Empty_NA = c(NA, NA, NA),
  Empty_String = c("", "", ""),
  Score = c(90, 85, 95)
)

# Remove empty columns with verbose output
cleaned_df <- rm_cols(df)

# Remove empty columns silently, keep original column name case
cleaned_df <- rm_cols(df, tolower = FALSE, verbose = FALSE)

}
